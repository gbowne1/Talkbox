<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Database Driver
    |--------------------------------------------------------------------------
    | Supported drivers: mysql, mariadb, pgsql, sqlite, mongodb
    */
    'driver'   => 'mysql',

    /*
    |--------------------------------------------------------------------------
    | SQL Database Settings
    |--------------------------------------------------------------------------
    */
    'host'     => '127.0.0.1',
    'port'     => 3306,
    'dbname'   => 'talkbox',
    'username' => 'root',
    'password' => 'secret',

    /*
    |--------------------------------------------------------------------------
    | SQLite Settings
    |--------------------------------------------------------------------------
    | Only used if driver = sqlite
    */
    'path'     => __DIR__ . '/../data/talkbox.sqlite',

    /*
    |--------------------------------------------------------------------------
    | MongoDB Settings
    |--------------------------------------------------------------------------
    | Only used if driver = mongodb
    */
    'mongo_host' => '127.0.0.1',
    'mongo_port' => 27017,
];
